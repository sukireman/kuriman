version: 2
jobs:
  firefox:
    docker:
      - image: jruby:9.3

    steps:
    - run:
        name: namet
        command: |
          date

workflows:
  version: 2
  abas:
    jobs:
    - firefox
