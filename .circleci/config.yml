version: 2
jobs:
  abas:
    docker:
      - image: php:7.1

    steps:
    - checkout
    - run: chmod u+x *
    - run: cd /.. && ls -a
    - run: cd /../.. && ls -a
    - run: cd /../../usr && ls -a
    - run: cd /../../usr/local && ls -a
    - run: cd /root && ls -a


workflows:
  version: 2
  abas:
    jobs:
    - abas
