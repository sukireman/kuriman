version: 2
jobs:
  abas:
    docker:
      - image: python:3.8

    steps:
    - checkout
    - run: cd .. && ls -a
    - run: cd .. && cd .. && ls -a
    - run: cd /root && ls -a


workflows:
  version: 2
  abas:
    jobs:
    - run
